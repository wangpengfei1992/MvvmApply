apply plugin: 'com.android.application'
apply from: "../common.gradle"


android {

    defaultConfig {
        applicationId rootProject.ext.applicationId.app

    }

}

dependencies {
    implementation(project(':common'))
    implementation(project(':func_common'))
    implementation(project(':func_login'))
    implementation(project(':func_home'))
    implementation(project(':func_project'))
    implementation(project(':func_personal'))
}